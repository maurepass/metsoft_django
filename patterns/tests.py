
from django.test import TestCase

# Create your tests here.
from django.urls import reverse


class PatternsTest(TestCase):

    # def test_url_patterns(self):
    #     response = self.client.get(reverse('patterns'))
    #     self.assertEqual(response.status_code, 200)
    #
    # def test_url_pattern_card(self):
    #     response = self.client.get(reverse('pattern-card'))
    #     self.assertEqual(response.status_code, 200)
    #
    # def test_url_pattern_edit(self):
    #     response = self.client.get(reverse('pattern-edit'))
    #     self.assertEqual(response.status_code, 200)
    #
    # def test_url_pattern_create(self):
    #     response = self.client.get(reverse('pattern-create'))
    #     self.assertEqual(response.status_code, 200)
    #
    # def test_url_pattern_status_change(self):
    #     response = self.client.get(reverse('pattern-status-change'))
    #     self.assertEqual(response.status_code, 200)
    #
    # def test_url_patterns_report(self):
    #     response = self.client.get(reverse('pattern-report'))
    #     self.assertEqual(response.status_code, 200)
    pass